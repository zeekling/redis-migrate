kind: pipeline
type: exec
name: default
steps:
- name: code-analysis
  image: aosapps/drone-sonar-plugin
  settings:
    sonar_host:
      from_secret: sonar_host
    sonar_token:
      from_secret: sonar_token
- name: build
  commands:
  - make clean && make
trigger:
  branch:
    - master
  event:
    - pull_request
    - push
